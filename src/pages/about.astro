---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle: string = "Sobre mi";

const identity: object = {
  firstName: "Sebastian",
  country: "Colombia",
  occupation: "Frontend Developer",
  hobbies: ["VideoJuegos", "Tocar guitarra", "Montar bicicleta"],
};

const skills = [
  "HTML",
  "CSS",
  "JavaScript",
  "TypeScript",
  "React",
  "Astro",
  "Wordpress",
  "Strapi",
  "jQuery",
  "TailwindCSS",
  "PHP",
];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "crimson";
const fontWeight = "800";
const textCase = "uppercase";
---

<BaseLayout pageTitle="Sobre mí">
  <p>I'm practice with Astro Framework, until now, is easy and simple.</p>

  <p>He aquí algunos datos sobre mí:</p>
  <ul>
    <li>Me llamo {identity.firstName}.</li>
    <li>Vivo en {identity.country} y trabajo como {identity.occupation}.</li>
    {
      identity.hobbies.length >= 2 && (
        <li>
          Dos de mis aficiones son: {identity.hobbies[0]} y{" "}
          {identity.hobbies[1]}
        </li>
      )
    }
  </ul>

  <p>Mis habilidades son:</p>

  <ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>

  {happy && <p>¡Aprender Astro es muy sencillo!</p>}

  {finished && <p>¡He terminado este tutorial!</p>}

  {
    goal === 3 ? (
      <p>Mi objetivo es terminar en 3 días.</p>
    ) : (
      <p>Mi objetivo no son 3 días.</p>
    )
  }
</BaseLayout>
